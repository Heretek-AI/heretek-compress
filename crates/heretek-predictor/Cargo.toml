[package]
name = "heretek-predictor"
version = "0.1.0"
edition = "2021"

[dependencies]
heretek-error = { path = "../heretek-error" }
candle-core = "0.10"
candle-nn = "0.10"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
